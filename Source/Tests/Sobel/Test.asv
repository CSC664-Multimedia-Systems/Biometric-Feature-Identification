subject = imread("subject.PNG");
expected = imread("expected.PNG");
expected = im2gray(expected);

gray = im2gray(subject);
test = sobel(gray);
%test = uint8(test);
sobel_img = sobel(gray);


result = expected - test;